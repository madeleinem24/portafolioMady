/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Tailwind CSS v4 — procesamiento CSS-first via @theme en globals.css
    '@tailwindcss/postcss': {},
  },
}

export default config
